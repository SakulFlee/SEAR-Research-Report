%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% 001 - Packages
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%% Necessary / Mandatory packages
% Language packages
\usepackage[english]{babel}

%% Special font encodings
% http://tex.stackexchange.com/questions/664/why-should-i-use-usepackaget1fontenc
% http://dsanta.users.ch/resources/type1.html
% http://tex.stackexchange.com/a/44699
% http://tex.stackexchange.com/a/44701
\usepackage[T1]{fontenc}
\usepackage{ae,aecompl}
\usepackage[utf8]{inputenc}

% BibTex
\usepackage[backend=bibtex,style=authoryear,bibstyle=authoryear,
sorting=nyt,firstinits=true]{biblatex}
\addbibresource{sources.bib} 

% Date utils
\usepackage[nodayofweek,level]{datetime}

% Include 'mathptmx' (Times Roman font)
\usepackage{mathptmx}

% Include 'Helvetica' (Arial font)
\usepackage[scaled=.90]{helvet}

%% Use 'Latin Modern', an improved version of the 'Computer Modern' font
\usepackage{lmodern}

%% File contents
\usepackage{filecontents}

%% TikZ and PGF for graphics
%% [Note: needs filecontents]
\usepackage{tikz}
\usepackage{tikzscale}
\usepackage{pgf}

%% XColor package for colors and 'hyperref' link colors
\usepackage{xcolor}

%% Hyperref package for enabling clickable references
%% [Note: 'hidelinks' option removes borders from links]
\usepackage[hidelinks]{hyperref}

%% Units package for settings units in a typographically correct way
%% [Note: includes 'nicefrac']
\usepackage{units}

%% Enable for modifying the page-layout
\usepackage{fancyhdr}

% Use listings for source listings
\usepackage{listings}

% Automatically create a new page for a section
\usepackage{titlesec}

% Package for including and captioning graphics
\usepackage{graphicx}

% Package for advancing captions [e.g. subcaptions]
\usepackage{subcaption}

% Package for wrapfig
\usepackage{float}

% Package to wrap figures in text
\usepackage{wrapfig}

%%% Optional packages
%% Don't indent paragraphs
\usepackage{parskip}

%% Glossaries and Acronyms
% \usepackage[acronym, toc, numberedsection, nopostdot, automake]{glossaries}

%% Enable dot drawing
% \usepackage{dot2texi}

%% For Gantt-Diagramms
% \usepackage{pgfgantt}

%% BibTex
%% [Note: needs filecontents]
% \usepackage[backend=bibtex, bibencoding=ascii]{biblatex}

%% Long Table for longer tables!
% [Note: Tables that can go over multiple pages and will not break]
\usepackage{longtable}

% SVG
\usepackage[svgpath=img/]{svg}

% Page split!
\usepackage{multicol}

% Landscape
\usepackage{lscape}

% For importing from other directories
\usepackage{import}

% Mathematical symbol expansion
\usepackage{amsmath}

% PlantUML
% \usepackage{plantuml}

% Verbatim input
\usepackage{verbatim}

% Array
\usepackage{array}

% TabularX
\usepackage{tabularx}

% Captions
\usepackage[hypcap=false]{caption}

% Additional symbols
\usepackage{enumitem,amssymb}

% To include PDF pages
\usepackage{pdfpages}

% Recommended for bibtex
\usepackage{csquotes}